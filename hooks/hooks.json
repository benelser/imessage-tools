{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "command -v imessage-tools >/dev/null 2>&1 || (cd \"${CLAUDE_PLUGIN_ROOT}\" && bun link 2>/dev/null)"
          }
        ]
      }
    ]
  }
}
